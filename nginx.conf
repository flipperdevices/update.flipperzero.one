server { 
    listen 80;

    location / {
        root /usr/share/nginx/html;
    }

    location /builds {
        root /;
        autoindex on;
    }

    location /directory.json {
        root /builds;
    }

    location /qFlipper {
        root /builds;
    }
}